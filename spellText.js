function spellText(text) {
    for (let i = 0 ; i < text.length ; i++) {
        console.log(text.charAt(i))
    }

    // for (let letter of text) {
    //     console.log(letter)
    // }

}
spellText("Ayra")

/* i = i + 1
i += 1
i++ */
